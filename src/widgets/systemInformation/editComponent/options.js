import { TYPES } from '../common'


const platform = {
  name: 'Platform',
  type: TYPES.PLATFORM,
}

const language = {
  name: 'Language',
  type: TYPES.LANGUAGE,
}

const screenSize = {
  name: 'Screen size',
  type: TYPES.SCREEN_SIZE,
}

export default [
  platform,
  language,
  screenSize,
]
